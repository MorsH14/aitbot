# ============================================================
# XAU/USD Scalping Bot — Environment Variables (Deriv.com)
# ============================================================
# 1. Copy this file to .env
# 2. Fill in your Deriv credentials
# 3. Never commit .env to git (already in .gitignore)

# ── Deriv App ID ───────────────────────────────────────────
# '1089' works for testing (shared Deriv demo app)
# Register your own free app at https://api.deriv.com/ for production
DERIV_APP_ID=1089

# ── Deriv API Token ────────────────────────────────────────
# 1. Go to https://app.deriv.com/
# 2. Account Settings → Security & Safety → API Token
# 3. Create a token with "Trade" + "Read" scope
# 4. Paste it here
DERIV_TOKEN=your_deriv_api_token_here

# ── Account Type ───────────────────────────────────────────
# 'demo' or 'real' — informational only
# The actual account type is determined by the token you use
ACCOUNT_TYPE=demo

# ── Multiplier ─────────────────────────────────────────────
# Deriv Multiplier leverage for XAU/USD
# Options: 10, 20, 30, 40, 50, 100, 200, 500
# Start with 100 for demo; higher = more risk
DERIV_MULTIPLIER=100

# ── Logging ────────────────────────────────────────────────
LOG_LEVEL=info
